# Διόρθωση: το PoP προβλέπει τη βροχή πριν ξεκινήσει

Διορθωμένη έκδοση του add-on `Greece-Sky-and-Weather-Nowcast` (baseline `1.0.0`).

Το παράπονο ήταν ότι **το `pop_60m` δεν ανέβαινε πριν ξεκινήσει η βροχή, αλλά
αφού είχε ήδη ξεκινήσει**. Αυτό αναπαράχθηκε και διορθώθηκε, και τα νούμερα
παρακάτω προέρχονται από το **πραγματικό entry point** (`python3 main.py`) του
add-on — όχι από μονωμένα unit tests.

## Τι έφταιγε

Πέντε ανεξάρτητες αιτίες, όλες στο PoP path:

1. **Τα προ-βροχικά σήματα δεν έφταναν ποτέ στο μοντέλο.**
   Το base μοντέλο έχει 38 χαρακτηριστικά, αλλά μόνο **7** έχουν μη μηδενικό
   βάρος — και είναι όλα **απόλυτες** τιμές:

   ```
   humidity          +0.4011
   absolutepressure  -0.1130
   pressure          -0.0906
   temperature       -0.0882
   windgust          +0.0693
   windspeed         +0.0635
   solarradiation    -0.0655
   ```

   Όλα τα trends είναι **ακριβώς μηδέν**: `dP_10m`, `dP_30m`, `dRH_10m`,
   `dRH_30m`, `rainrate`, `hourlyrain`, `vpd`, `dewpoint_c` → `0.000000`.

   Αυτό είναι ο πυρήνας του συμπτώματος. Το μοντέλο βλέπει μόνο απόλυτη
   υγρασία/πίεση, οπότε **δεν μπορεί να δει τη ράμπα πριν τη βροχή** — βλέπει
   την υγρασία να ανεβαίνει *καθώς* αρχίζει η βροχή. Εξ ου και η βηματική
   συνάρτηση 1.3% → 93% στο πρώτο λεπτό της βροχής: δεν «προβλέπει», απλώς
   αντιδρά. Επιβεβαιώθηκε διαβάζοντας απευθείας τα βάρη του
   `model/base_model.json`.

2. **Το `loop.py` έδινε λάθος παράθυρα.** Περνούσε `buffer.last_n(11)` ως
   «πρόσφατο ιστορικό», οπότε τα «30m» deltas ήταν στην πραγματικότητα
   ~10 δείγματα. Δύο διαφορετικά ονόματα για το ίδιο παράθυρο.

3. **Η online εκπαίδευση έσβηνε ό,τι μάθαινε.** Με `batch=1` και `l2`
   διαιρεμένο με `n`, το διάνυσμα βαρών συρρικνωνόταν σε κάθε ωριαίο update
   (`||w|| 6.1644 → 5.6713` μετρημένο).

4. **Η εκπαίδευση χαλούσε το προεκπαιδευμένο μοντέλο.** Τα βάρη των trends
   έπαιρναν **λάθος πρόσημο** (`w60[dRH_10m] → −0.4088`, ενώ σωστό είναι
   θετικό), δηλαδή το μοντέλο μάθαινε «αν πέφτει η υγρασία, έρχεται βροχή».

5. **Τα labels ήταν μεροληπτικά.** Από 63 δείγματα, τα 16/46 θετικά αφορούσαν
   βροχή που **είχε ήδη ξεκινήσει**. Επιπλέον το `rain_1m_mm` έλειπε από τον
   feature vector, οπότε το μοντέλο δεν μπορούσε να μάθει persistence.

## Τι άλλαξε

| Αρχείο | Αλλαγή |
|---|---|
| `core/features.py` (νέο) | Lookbacks με **offset σε λεπτά** (`minutes_ago`) αντί για index. Προστέθηκαν `dewpoint_c`, `spread_td`, `rain_1m_mm`, `pre_rain_index`, `instability_index`. |
| `model/engine.py` | Ο feature vector επεκτείνεται με 17 τοπικά χαρακτηριστικά. Προστέθηκαν **φυσικά priors** (πίεση ↓, υγρασία ↑, ηλιακή ↓, κορεσμός, persistence βροχής). **Παγώνουν** τα base βάρη. Προστέθηκαν replay memory και class balancing. **Νέο:** ανοχή σε state παλιάς έκδοσης (βλ. παρακάτω). |
| `ml/online.py` | Το `l2` δεν διαιρείται πλέον με `n`. Προστέθηκε `trainable` mask ώστε να προσαρμόζονται **μόνο** τα τοπικά προ-βροχικά βάρη. |
| `core/loop.py` | `buffer.get_all()` αντί `last_n(11)`. Το training καλύπτει και τον 30m ορίζοντα. Ring buffer `420` → `900` λεπτά. |

Σημείωση: το upstream υπολόγιζε τα derived features **inline** μέσα στο
`loop.py` — δεν υπήρχε `core/features.py`. Το νέο αρχείο είναι **προσθήκη**,
όχι αντικατάσταση.

## Αποτελέσματα (πραγματικό `python3 main.py`)

1400 λεπτά, βροχές στα λεπτά 600 και 1200 (διάρκεια 60m, με φυσική ράμπα
πίεσης/υγρασίας 45 λεπτών πριν από κάθε επεισόδιο). Το `pop_60m` συγκρίνεται με
ground truth «βροχή στα επόμενα 60 λεπτά».

### Πριν τη βροχή

| `pop_60m` στα | ORIGINAL @600 / @1200 | FIXED @600 / @1200 |
|---|---|---|
| −45m | 0.5% / 0.6% | 0.9% / 5.2% |
| −35m | 0.6% / 0.7% | **12.9% / 56.5%** |
| −25m | 0.8% / 0.8% | **33.8% / 80.8%** |
| −15m | 0.9% / 0.8% | **63.7% / 94.1%** |
| −5m | 1.2% / 0.8% | **73.3% / 96.8%** |
| Έφτασε 50% πριν τη βροχή; | **όχι, σε κανένα** | ναι: **19m** και **35m** νωρίτερα |

### Πάνω στη βροχή (το αρχικό σύμπτωμα)

| | ORIGINAL | FIXED |
|---|---|---|
| `pop_60m` στο t−1 | 1.3% | 77.8% |
| `pop_60m` στο t+1 | 93.0% | 96.7% |
| **άλμα** | **+91.7 μονάδες** | +18.9 μονάδες |

Το original κάνει βηματική συνάρτηση **ακριβώς πάνω στη βροχή**: 1.3% → 93%
σε ένα λεπτό. Αυτό είναι το «προβλέπει αφού ξεκινήσει». Το fixed έρχεται στη
βροχή **έχοντας ήδη ανέβει**, οπότε το άλμα είναι μικρό.

### Ανά φάση (`pop_60m ≥ 50%`)

| Φάση | λεπτά | ORIGINAL | FIXED |
|---|---|---|---|
| Ήσυχη περίοδος (700–1140) | 440 | 4.8% | **0.0%** |
| Ράμπα πριν τη βροχή | 90 | **0.0%** | **60.0%** |
| Όσο βρέχει | 120 | 57.5% | 95.0% |

Δηλαδή το fixed **κερδίζει 60 μονάδες κάλυψης στη ράμπα** χωρίς να αυξήσει τα
false alarms στην ήσυχη περίοδο (τα μηδενίζει).

### Σύνολο (threshold 50%, ground truth = βροχή στα επόμενα 60m)

| | precision | recall | F1 |
|---|---|---|---|
| ORIGINAL | 0.00 | 0.00 | 0.00 |
| FIXED | 0.33 | 0.46 | 0.38 |

Το ORIGINAL έχει **μηδέν** TP: όποτε ενεργοποιείται το 50%, η βροχή έχει ήδη
ξεκινήσει, άρα δεν μετράει ως πρόβλεψη. Το FIXED είναι το πρώτο που παράγει
πραγματικές προειδοποιήσεις.

## Εγκατάσταση

Το `features.py` **δεν υπάρχει** στο upstream — προστίθεται.

```
nowcast_ml/rootfs/app/core/features.py   (ΝΕΟ)
nowcast_ml/rootfs/app/core/loop.py       (αντικατάσταση)
nowcast_ml/rootfs/app/ml/online.py       (αντικατάσταση)
nowcast_ml/rootfs/app/model/engine.py    (αντικατάσταση)
```

Ο υπόλοιπος κώδικας παραμένει ως έχει. Το `config.json`, το `Dockerfile` και
το `run.sh` δεν αλλάζουν.

### Υπάρχουσα εγκατάσταση (υπάρχον `station_model.json`)

Το `/data/station_model.json` παλιάς έκδοσης έχει ένα βάρος ανά **base**
χαρακτηριστικό (38), χωρίς τα 17 νέα (55 συνολικά). Όταν το νέο `engine.py` το
διαβάσει:

* συντηρεί τα **δικά σας εκπαιδευμένα** base βάρη,
* και **επαναεφαρμόζει τα priors** πάνω τους.

Χωρίς αυτό, ένα απλό zero-pad θα μηδένιζε τα priors και θα επανέφερε σιωπηλά
το lag που διορθώνει αυτή η έκδοση. Το `base_version` **δεν** αλλάζει, οπότε ο
υπάρχων έλεγχος συμβατότητας δεν το έπιανε. Η επαναφορά καταγράφεται στο log:

```
INFO: Upgraded pop weights for h=60 from 38 base coefficients: priors re-applied.
```

Δεν χρειάζεται να σβήσετε το `station_model.json`. Αν θέλετε καθαρή εκκίνηση,
σβήστε το — η συμπεριφορά από το πρώτο λεπτό είναι ίδια, γιατί τα priors είναι
φυσικά και όχι αποτέλεσμα εκπαίδευσης.

## Τι ελέγχθηκε

| Έλεγχος | Αποτέλεσμα |
|---|---|
| `python3 main.py` από πλήρες add-on tree (1400 ticks) | καθαρή έξοδος, 0 γραμμές stderr |
| Original vs fixed, ίδια χρονοσειρά | 19m/35m lead έναντι «ποτέ» |
| συχη περίοδος | 0.0% false alarms |
| Αναβάθμιση με **παλιό** `station_model.json` | priors επανεφαρμόζονται, 19m/35m lead διατηρείται |
| Round-trip save/load | ταυτόσημα PoP και quantile βάρη |
| Επανεκκίνηση (boot με υπάρχον state) | ξεκινά κανονικά, χωρίς σφάλματα |
| MQTT ενεργό (`mqtt_enabled: true`) | συνδέεται/αποτυγχάνει καθαρά, χωρίς crash |
| `learning_enabled: false` | training παραλείπεται σωστά |
| `persist_model: false` | **δεν** γράφεται state (σωστό) |
| `log_level: debug` | χωρίς crash |
| Compile όλων των αρχείων | OK |

### Ανθεκτικότητα σε θόρυβο

Τα παραπάνω είναι «καθαρή» προσομοίωση. Για να μην κρύβεται υπερπροσαρμογή στο
ένα timeline, δοκιμάστηκε και **άλλο** σενάριο: 4 άνισα επεισόδια (λεπτά 455,
980, 1520, 2050, διάρκειες 35/80/50/70) και **αυτοσυσχετισμένος** θόρυβος στα
αισθητήρια (random walk, lag-1 autocorrelation ≈ 0.85 — όπως ένα πραγματικό
βαρόμετρο, όχι λευκός θόρυβος που θα κατέστρεφε κάθε 10/30m delta).

| Σενάριο | ORIGINAL lead | FIXED lead |
|---|---|---|
| Άνισα events, χωρίς θόρυβο | ποτέ / ποτέ / ποτέ / ποτέ | **20 / 36 / 38 / 40 min** |
| Άνισα events, θόρυβος 0.05 | ποτέ / ποτέ / ποτέ / ποτέ | **19 / 37 / 42 / 38 min** |

Το lead του fixed είναι **σταθερό** και στα δύο σενάρια, δηλαδή δεν εξαρτάται
από το ένα συγκεκριμένο timeline ούτε από το ότι ο προσομοιωτής είναι
απολύτως καθαρός. Το original δεν ενεργοποιείται πριν τη βροχή ούτε με
μέγιστο 28% στη ράμπα.

## Περιορισμοί — τι ΔΕΝ λέει αυτό

* Τα νούμερα προέρχονται από **προσομοιωμένη** βάση. Ο θόρυβος έχει ελεγχθεί
  (βλ. παραπάνω) αλλά παραμένει προσομοίωση: πραγματικά όργανα έχουν spikes,
  offsets και σφάλματα βαθμονόμησης που δεν μοντελοποιήθηκαν.
* Η ράμπα προέρχεται από σήματα που ο προσομοιωτής παράγει **σκόπιμα** 45
  λεπτά πριν τη βροχή. Σε πραγματικά δεδομένα η προειδοποίηση θα είναι
  μικρότερη και λιγότερο σταθερή.
* Το F1 του 60m ορίζοντα (0.38) είναι **μέτριο**. Είναι όμως σαφώς καλύτερο από
  μηδέν, και το κρίσιμο κέρδος είναι η συμπεριφορά πριν τη βροχή.
* Ο 360m ορίζοντας παραμένει αδύναμος: με base rate βροχής ~67% στο harness το
  πρόβλημα εκφυλίζεται.
* Δεν έχει γίνει δοκιμή με πραγματικά δεδομένα σταθμού. Συνιστάται να τρέξετε
  σε shadow mode πριν βασιστείτε σε αυτό.